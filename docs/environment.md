# §10. Verified environment facts

> Split out of `CLAUDE.md` on 2026-08-11. **Section numbers are unchanged** — every
> `§10` reference in the repository points here.
>
> Read this *before* writing a probe script: a fact already recorded here does not need
> re-probing. Add anything a probe teaches you back into the table below, so the next
> session can skip the probe entirely. Companion file: `docs/running-commands.md` (§9.1).

Measured on the user's machine. Reuse these; re-verify before contradicting them.

| Fact | Value |
|---|---|
| Windows | 11 Home Single Language, 10.0.22631 |
| WSL | 2.7.11.0, kernel `6.18.33.2-microsoft-standard-WSL2` |
| Distro | Ubuntu **26.04 "resolute"**, WSL version 2. Login user **`shinarus`**, `$HOME` = `/home/shinarus` |
| Shells | `bash` **5.3.9(1)-release**; `/bin/sh` → **`dash`** |
| `touch -d` vs `date -d` disagree on timezone (found lesson 6) | Given the identical bare string `"YYYY-MM-DD HH:MM"`, uutils **`touch -d`** parses it as **UTC**, while uutils **`date -d`** parses the same string as **local time** (machine TZ = Asia/Bangkok, UTC+7). Verified live in a WSL scratch dir, not from a lesson's own captured commands. A lesson that sets a timestamp with `touch -d` and then reads it back with `date` (or `ls -l`, which renders local time) will see a 7-hour offset that looks like a bug but isn't — always cross-check with `date -u` or `stat` before writing a lesson `cal` about a "wrong" timestamp |
| Interactive-shell aliases (`~/.bashrc`) | `ls='ls --color=auto'`, `grep`/`fgrep`/`egrep='… --color=auto'`, `ll='ls -alF'`, `la='ls -A'`, `l='ls -CF'`, `alert='notify-send …'`. Only loaded when bash is **interactive** — `bash -lc '...'` (this project's verification shell) is login but non-interactive and does **not** source `~/.bashrc`, so it never sees them. Consequence confirmed 2026-08-16 (lesson 4): `type ls` / `command -v ls` verified via `bash -lc` return the real path, but the learner's actual interactive terminal returns `ls is aliased to \`ls --color=auto'` instead — always, reproducibly, not a fluke. Any lesson command that resolves a name (`type`, `command -v`, `alias`, a shell function) touching `ls`/`grep`/`fgrep`/`egrep` must be re-verified with `bash -ic` too, see `docs/running-commands.md` |
| Coreutils | **uutils** (Rust) — `/bin/ls` and `/usr/bin/[` symlink into `/usr/lib/cargo/bin/coreutils/`, package `coreutils-from-uutils` |
| `nc` (found lesson 24) | **OpenBSD netcat**, confirmed via `nc -h`. Its wait/quit flag is `-q seconds` (time to wait for EOF after stdin closes before quitting), not GNU netcat's `-w`/`-i` semantics — a lesson dissecting a captured `nc` invocation must dissect the flags actually present in `nc -h` on this machine, not assume GNU netcat's flag set |
| Packages | 776 installed, 2524.9 MB; `dpkg --print-architecture` = `amd64` |
| CPUs / RAM | 6 CPUs via `.wslconfig` (`nr_cpus=6` in cmdline). `MemTotal` = **5 036 144 kB ≈ 4.8 GiB** (re-measured 2026-08-08 — the older "8 GB" figure was the Windows-side allocation, not what the guest sees) |
| `/proc/cmdline` | `initrd=\initrd.img WSL_ROOT_INIT=1 panic=-1 nr_cpus=6 … console=hvc0 debug …` |
| Kernel boot time | `Freeing unused kernel image (initmem) memory: 4852K` at **0.376880 s** |
| Userspace boot time | `Startup finished in 2.456s (userspace)` |
| PID 1 | systemd |
| `/boot` | **empty** — no bootloader in WSL2 |
| Filesystem penalty | 500 × `touch`: `~` = **0.017 s**, `/mnt/c` = **0.882 s** → **52×** |
| `/dev/kvm` | exists (`crw-rw---- 1 root kvm 10, 232`) |
| `qemu-system-aarch64 -accel help` | lists **`tcg` only** — ARM64 on x86 is always emulation |
| Cross compiler | `aarch64-linux-gnu-gcc` 15.2.0; `arm-linux-gnueabihf-gcc` also installed |
| Toolchain versions | GCC **15.2.0** (`Ubuntu 15.2.0-16ubuntu1`), defaults to **C23**; GNU Make **4.4.1**; glibc **2.43** |
| Static vs dynamic hello | x86 dynamic **15,952 B**; x86 `-static` **816,912 B** (**51.2×**); ARM64 `-static` **705,328 B** |
| Shared-library facts | `libc.so.6` = **2,186,512 B**; `ldconfig -p` lists **485** libraries; static-vs-dynamic break-even ≈ **3** programs |
| ELF sample (lesson 18) | dynamic **16,184 B**; `.bss` **16,424 B** = `MemSiz − FileSiz` (0x4298 − 0x270); same program with a 1 MB *initialised* array → **1,064,584 B** (**65.8×**) |
| `strip` gain | static binary 817,000 → **735,512 B** = **10.0 %**; `-ffunction-sections -Wl,--gc-sections` on a 5-function program: 16,112 → 15,856 B = only **1.6 %** |
| Parallel make | `-j6` on 6 cores is **2.6×**, not 6× — link step is serial and gcc processes contend for header reads |
| Installed | `qemu-system-arm`, **`qemu-user`** (`/usr/bin/qemu-aarch64` — installed 2026-08-08 for lesson 29; §10 previously said otherwise), `gcc-aarch64-linux-gnu`, `gdb-multiarch`, `device-tree-compiler`, `u-boot-tools`, `tree`, `gpiod` + `libgpiod3` |
| Not installed | `qemu-system-x86_64`, `shellcheck`, `pahole` |
| QEMU version | **10.2.1** (`Debian 1:10.2.1+ds-1ubuntu3.2`), both `qemu-system-aarch64` and `qemu-aarch64`. `-M help` lists **113** ARM machines (114 lines incl. header); `virt` is an alias of **`virt-10.2`** |
| QEMU `virt` | has **no I2C/SPI bus** (`No 'i2c-bus' bus found`). `grep -icE 'i2c\|spi\|mmc\|sdhci\|usb\|ethernet'` on its dumped `.dts` → **0** for every term. Lesson 58 must use `i2c-stub` / `gpio-sim` / SPI loopback, or switch machine to `raspi3b` (`bcm2835-i2c`) / `mcimx7d-sabre` (`imx.i2c`) — both accept `-device tmp105`, but **neither supports `-machine dumpdtb`** (`This machine doesn't have an FDT`) |
| `virt` memory map (lesson 30) | flash `0x00000000` (2×64 MiB), GIC dist `0x08000000`, GIC cpu `0x08010000`, PL011 `0x09000000`, PL031 RTC `0x09010000`, fw-cfg `0x09020000`, PL061 GPIO `0x09030000`, **32** virtio-mmio slots `0x0a000000`→`0x0a003e00` step `0x200`, PCIe ECAM `0x10000000`, RAM `0x40000000` |
| `virt` device tree (lesson 30) | `dumpdtb` file is always exactly **1 048 576 B** (`totalsize` header field says the same); `dtc -I dtb -O dts` → **393** lines (391 after stripping `rng-seed`/`kaslr-seed`). `-m 1G` changes **1** line; `-M virt,gic-version=3` changes **16**; `-smp 2` changes **35** (phandle renumbering cascade). `-device virtio-net-device` / `virtio-blk-device` change **0** lines — the 32 slots are pre-declared |
| Bare-metal ARM64 (lesson 30) | `hello.S` + `link.ld` at `. = 0x40080000`, built `-nostdlib -static -Wl,-T,link.ld`: `.text` **105 B**, ELF **66 504 B**, entry `0x40080000`. Runs under `-kernel`, prints via PL011, parks in `wfi`. `info registers` → `PC=0x4008001c`, `X01=0x09000000`. `info jit` on it: TB count **6**, avg target **7 B**, avg host **158 B**, **expansion ratio 21.6** (vs **3.48** user-mode in lesson 29 — softmmu + MMIO + very short TBs) |
| Self-built toolchain (lesson 28) | crosstool-NG **1.28.0** (`ct-ng` tarball **2 448 288 B**) → `~/x-tools/aarch64-unknown-linux-musl`, **354 MB**, **34** tools in `bin/`, dir left `dr-xr-xr-x` by `CT_PREFIX_DIR_RO`. GCC **15.2.0**, binutils **2.45**, gdb **16.3**, musl **1.2.5**, kernel headers **6.16**, all stamped `(crosstool-NG 1.28.0)` |
| crosstool-NG build cost | total of all steps **3 591 s** (≈62 min wall on 6 cores, `build.6`). `cc_core` **860.44 s** + `cc_for_host` **1 066.55 s** = **53.7 %**; musl itself only **37.72 s** (**1.1 %**); tarball download **513.78 s**; cross-gdb **470.93 s**. `.build` peaks at **18 GB** with save-steps on; `build.log` **41 704 605 B** |
| musl vs glibc, same `temp_daemon.c` | `-static`: musl **108 720 B**, glibc **787 032 B** = **7.24×**. After `strip`: **42 512** vs **655 288** = **15.4×** (musl loses **60.9 %**, glibc only **16.7 %**). `size`: `.text` **39 300** vs **626 361**, `.bss` **1 792** vs **22 680**. musl dynamic **14 144 B**, interp `/lib/ld-musl-aarch64.so.1` |
| Prebuilt ARM64 kernel (lesson 32) | Debian trixie `linux-image-6.12.94+deb13-**cloud**-arm64_6.12.94-1_arm64.deb` = **27 985 180 B** → `boot/vmlinuz-…` = **30 771 136 B**, `file` says `Linux kernel ARM64 boot executable Image, little-endian, 4K pages`. The non-cloud variant is **92 732 600 B** deb / **37 605 312 B** image — cloud is smaller and boots `virt` fine. Find the current version with `curl -s https://deb.debian.org/debian/dists/trixie/main/binary-arm64/Packages.gz \| zcat \| awk …` (old versions get pulled from the pool) |
| Lesson 32 initramfs | `busybox-static_1.38.0-3_arm64.deb` **860 056 B** → binary **1 980 944 B**, static, stripped, **280** applets. 7-entry tree (`/init`, `/bin/busybox`, `/bin/sh`→busybox, `/dev`, `/proc`, `/sys`, `.`). Raw cpio `newc` is **always 1 982 464 B** (`3872 blocks`); the **gzip -9 result is NOT reproducible across rebuilds** — measured 1 035 400 / 1 035 399 / 1 035 396 B, because cpio headers embed inode numbers and mtimes. Repacking the *same* tree 3× is byte-identical |
| Lesson 32 boot | `-M virt -cpu cortex-a57 -m 512 -kernel Image -initrd … -append "console=ttyAMA0 rdinit=/init" -nographic` → **238** log lines, stable marker line numbers **2** `Linux version`, **5** `Machine model: linux,dummy-virt`, **38** `Kernel command line`, **90** `Memory: 411880K/524288K available (14272K kernel code, 2784K rwdata, 10748K rodata, 2112K init, 908K bss, 43816K reserved, 65536K cma-reserved)`, **115** `printk: legacy console [ttyAMA0] enabled`, **156** `Trying to unpack rootfs image as initramfs...`, **162** `Freeing initrd memory: 1008K`, **229** `Run /init as init process`. `Run /init` timestamp is **not** stable — measured 1.61 s / 1.71 s / 2.06 s / 3.27 s depending on host load. Guest `MemTotal: 483592 kB` |
| Lesson 32 failure modes (all hit for real) | no `-initrd` → `VFS: Cannot open root device "" … error -6` then `Kernel panic … VFS: Unable to mount root fs on unknown-block(0,0)`; `rdinit=/sbin/init` (nonexistent) → **the same panic, no distinct message**; `--format=odc` → `Initramfs unpacking failed: incorrect cpio method used: use -H newc option` then same panic; `/init` without `+x` → **boots to a shell anyway** (kernel falls through to `/bin/sh`), no `procfs`, no applet symlinks; `init` that returns → `Kernel panic … Attempted to kill init! exitcode=0x00000000`; no `console=` → still **15 351 B** of log because `chosen/stdout-path = "/pl011@9000000"`; default `-cpu` with a real `Image` → **silent hang, 69 B, rc=124** (no error message at all, unlike the ELF case in lesson 31) |
| initrd page-rounding (lesson 32) | `linux,initrd-end − linux,initrd-start` = the initramfs file size **exactly**. But `Freeing initrd memory:` reports the page-aligned-down span: end `0x480fcc84` → `0x480fc000`, i.e. `0xfc000` = 1 032 192 B = **1008 KiB** |
| crosstool-NG gotchas (all hit for real) | gdb step needs a binary literally named `python` (`python-is-python3`); `paths.sh` caches `export python=""` from the first `./configure`, so re-run `./configure --enable-local && make` after installing it. `ct-ng <step>+` refuses to resume unless `CT_DEBUG_CT_SAVE_STEPS` was on *before* the build, and refuses if `.config` changed since |
| `/proc`'s own hard-link count is a kernel task counter, not a subdirectory count (lesson 5) | `stat -c %h /proc` (or the link-count column of `ls -l /`) does **not** follow the usual "2 + number of visible child directories" rule procfs otherwise looks like it should obey. Verified live: adding 5 new background processes caused **zero** immediate change in the nlink value; `ps -eLf \| wc -l` (visible threads in this PID namespace, ≈78) is far short of the nlink figure (≈220–254); `/proc/loadavg`'s total-tasks field (`running/total`) tracks the nlink value far more closely, off by a small roughly-constant delta (~6 in one 5-sample run). Conclusion: the number is an internal kernel-wide task/thread counter, not something derivable by counting entries `ls /proc` shows you. Do not build a lesson `cal` on "N processes → N+2 links" for `/proc` |
| Process count is a function of *when* you measure (lesson 9 / bt-09) | `ps -e --no-headers \| wc -l` gives **56–57** the moment you enter WSL but **26–27** thirty seconds later. Investigated with eight `ps -e -o comm` snapshots 10 s apart diffed by `comm -23`: what leaves in one batch is **28 `(udev-worker)`** processes plus `snapd`, then `systemd-timedat`. The burst is triggered by the distro starting, not by host boot — `uptime` at the first snapshot was 8 s. **Never build an exercise item on the absolute count**; quote it with the measurement time or not at all |
| Load average is an EMA with a 60 s time constant (lesson 9 / bt-09) | Four busy loops on 6 cores, starting from 0.47, do **not** reach 4.00: measured 30 s→**1.86**, 60 s→**2.71**, 90 s→**3.22**, 120 s→**3.53**, 150 s→**3.71**, 180 s→**3.83**. `L(t) = 4 − 3.53·e^(−t/60)` matches **every one** of the six samples to within 0.01. 5 s after killing them, `/proc/loadavg` still reads **3.52** while `top` shows `%Cpu(s): 100.0 id` and `1 running` — load and CPU-idle disagreeing is correct, not a bug |
| RSS double-counts shared pages (lesson 9 / bt-09) | Two identical `sleep` processes: `Rss: 7856 kB` / `7780 kB` but `Pss: 2923 kB` / `2842 kB`, with `Shared_Clean: 6344 kB` — summing RSS overcounts by ≈**2.7×**. Meanwhile `free -m` reports `used 512` against a total RSS of **238 932 kB**, because kernel slab/page-tables belong to no process's RSS. Sum-of-RSS is neither an upper nor a lower bound; use `Pss` from `/proc/*/smaps_rollup` |
| ARM64 `Image` header (lesson 33) | First 64 B of the Debian `Image`: `xxd` → `4d5a 40fa 3b28 6214 0000 0000 0000 0000` / `0000 e501 0000 0000 0a00 0000 0000 0000` / 16 zero bytes / `0000 0000 0000 0000 4152 4d64 4000 0000`. `od -A d -t x4` reads the same bytes as `fa405a4d 1462283b 00000000 00000000` / `01e50000 00000000 0000000a 00000000` / zeros / `00000000 00000000 644d5241 00000040`. So `text_offset` = **0**, `image_size` = `0x01e50000` = **31 784 960 B** against a file of **30 771 136 B** → a **1 013 824 B** BSS gap the bootloader must leave free; `flags` = `0xa` (LE, 4 KB pages, load anywhere); magic `ARM\x64` at 0x38; `pe_offset` `0x40`. `code0` = `0x91005a4d` reads as `MZ` for UEFI **and** as a harmless `ccmp x18, #0x0, #0xd, pl` for the CPU |
| QEMU's auto-generated boot stub (lesson 33) | With `-kernel`, at reset `PC=0000000040000000`, every X register **0**, `PSTATE=00000000400003c5 -Z-- EL1h FPU disabled`. `xp/10xw 0x40000000` → `0x580000c0 0xaa1f03e1 0xaa1f03e2 0xaa1f03e3 0x58000084 0xd61f0080 0x48200000 0x00000000 0x40200000 0x00000000` — **6 instructions + two 64-bit constants = 40 B**. GDB `x/6i $pc`: `ldr x0, 0x40000018` · `mov x1, xzr` · `mov x2, xzr` · `mov x3, xzr` · `ldr x4, 0x40000020` · `br x4`. At `break *0x40200000`: `x0 = 0x48200000`, x1=x2=x3=0, and `x/4xw $x0` → `0xedfe0dd0 0x00001000 0x40000000 0x0c1c0000` (DTB magic, little-endian). **Without** `-kernel` there is no stub: `xp/8xw 0x40000000` → `0xedfe0dd0 0x00001000 0x40000000 0xc81b0000 0x30000000 0x11000000 0x10000000 0x00000000` — the DTB sits at RAM base on its own |
| Breaking the ARM64 handover contract is **silent** (lesson 33) | Control run of lesson 32's kernel: **237 lines / 15 231 B** of log, ending at the `~ #` prompt. Under `-s -S` + `break *0x40200000`, `set $x0 = 0` then `continue` → **0 lines / 0 B**. `set $x0 = 0x44000000` (RAM, but no DTB there) → also **0 lines / 0 B**. No panic, no message, no exit code — a violated boot protocol looks exactly like dead hardware |
| U-Boot source (lesson 34) | `https://github.com/u-boot/u-boot.git`, tag **v2026.07**, commit **ece349ad** "Prepare v2026.07". `git clone --depth 1 -b v2026.07` = **109.91 s**, **402M**, `Updating files: 100% (38022/38022), done.` → **38 022** tracked files, **13 161** `.c`/`.h`, **3 951 888** lines of C. **1 522** files match `configs/*_defconfig` (`ls configs/` counts 1 563 entries — the other 41 are `.config` fragments, so **always glob `*_defconfig`**); of those, **546** enable SPL, **41** TPL, **1 233** are ARM |
| U-Boot `qemu_arm64_defconfig` (lesson 34) | `make qemu_arm64_defconfig` **2.90 s** → `.config` **1 860** lines. Key symbols: `CONFIG_ARM64=y`, `CONFIG_POSITION_INDEPENDENT=y`, `CONFIG_TEXT_BASE=0x00000000`, `CONFIG_DEFAULT_DEVICE_TREE="qemu-arm64"`, `CONFIG_OF_BOARD=y`, `CONFIG_SYS_LOAD_ADDR=0x40200000`, `CONFIG_BOOTDELAY=2`, `CONFIG_BOOTCOMMAND="bootflow scan -lb"`, `CONFIG_SYS_PROMPT="=> "`. **No SPL, no TPL** |
| U-Boot build cost (lesson 34) | `make -j6` with `CROSS_COMPILE=aarch64-linux-gnu- ARCH=arm64`: **REAL 35.38 s · USER 150.01 s · SYS 21.78 s · MAXRSS 82 668 kB**, **853** log lines, tree 414M → **481M**. Products: `u-boot` **10 654 232 B** (`ELF 64-bit LSB pie executable, ARM aarch64, statically linked, with debug_info, not stripped`), `u-boot.bin` **1 498 688 B** (`file` says just `data`), `u-boot-nodtb.bin` 1 498 688, `u-boot.srec` 4 303 720, `System.map` 218 243. `size u-boot` → text **1 434 148**, data **62 768**, bss **0**, dec **1 496 916**. Rebuild after a one-line patch: **4.19 s / 47** log lines |
| U-Boot on `virt` (lesson 34) | `-bios u-boot.bin` → banner `U-Boot 2026.07 (Aug 16 2026 - 12:12:35 +0700)`, `DRAM:  512 MiB`, `Core:  51 devices, 14 uclasses, devicetree: board`, `Flash: 64 MiB`, a bad-CRC env warning, `Net: eth0: virtio-net#32`, `Hit any key to stop autoboot:  0`, then `bootflow scan -lb` ends `(0 bootflows, 0 valid)` after DHCP takes `10.0.2.15` and TFTP fails with `TFTP error: 'Access violation' (2)` / `fatal: no kernel available`. `version` → `aarch64-linux-gnu-gcc (Ubuntu 15.2.0-16ubuntu1) 15.2.0` + `GNU ld (GNU Binutils for Ubuntu) 2.46`. `bdinfo` → DRAM `0x40000000` size `0x20000000`, flashstart 0x0 size `0x4000000` offset `0x16de40`, `relocaddr 0x5f690000`, `fdt_blob 0x5e54fd80`, `devicetree = board`, `serial addr 0x09000000`, `TLB addr 0x5ffe0000`, ethaddr `52:54:00:12:34:56`, baudrate 115200, Build = 64-bit. `printenv` → `fdt_addr=0x40000000`, `kernel_addr_r=0x40400000`, `loadaddr=0x40200000`, `ramdisk_addr_r=0x44000000`, `scriptaddr=0x40200000`, `pxefile_addr_r=0x40300000`, `boot_targets=qfw usb scsi virtio nvme dhcp`, `preboot=usb start`, `Environment size: 548/262140 bytes` |
| `-bios` vs `-kernel` geometry (lesson 34) | `-bios`: the file lands in flash at physical **0** and `PC=0` — `xp/4xw 0` → `0x1400000a 0xd503201f 0x00000000 0x00000000` (U-Boot's first branch), while `xp/4xw 0x40000000` is the DTB. `-kernel`: the file lands at **0x40200000** and `PC=0x40000000` (QEMU's stub, above). The two options put the same bytes in completely different places — this is the whole reason U-Boot is loaded with `-bios` |
| U-Boot patch workflow (lesson 34) | Demo patch adds `printf("Board: QEMU virt, Embedded Linux course build\n");` at the top of `board_late_init()` in `board/emulation/qemu-arm/qemu-arm.c`; `git format-patch -1` → **797 B**, hunk `@@ -104,6 +104,8 @@`. After rebuild the banner prints between `Err: serial,vidconsole` and `No USB controllers found`, and the version string becomes `U-Boot 2026.07-00001-g00a40294e789`. **Applying it twice** (both hit for real): `patch -p1` → `Reversed (or previously applied) patch detected!  Assume -R? [n]` … `1 out of 1 hunk ignored -- saving rejects to file …qemu-arm.c.rej` (366 B), **rc=1**; `git am` → `error: patch failed: …:104` / `error: …: patch does not apply` / `Patch failed at 0001 …`, **rc=128**, and `git am --abort` restores a clean tree. `git am` on a tree with no git identity → `fatal: empty ident name` |
| U-Boot build dependencies (installed 2026-08-16 for lesson 34) | `libgnutls28-dev`, `uuid-dev`, `swig`. Without the first, the build dies late — after ~30 s of successful compiling — with `tools/mkeficapsule.c:20:10: fatal error: gnutls/gnutls.h: No such file or directory` / `make[1]: *** [scripts/Makefile.host:113: tools/mkeficapsule.o] Error 1` / `make: *** [Makefile:2209: tools] Error 2` |
| QEMU monitor / CLI errors hit while verifying lessons 33–34 | `x/…` on the monitor → `0x40000000: Asm output not supported on this arch` (use GDB to disassemble ARM64, the monitor cannot). `-nographic` together with `-monitor stdio` → `cannot use stdio by multiple character devices` / `could not connect serial device to character backend 'stdio'`. GDB `target remote :1234` with no `-s` on the QEMU line → `could not connect: Connection timed out.` Missing files: `qemu-system-aarch64: could not load kernel 'NoSuchImage'` vs `qemu-system-aarch64: Could not find ROM image 'nosuch.bin'` — different wording for `-kernel` and `-bios`. `CROSS_COMPILE=aarch64-linux-gnu` (no trailing dash) → `/bin/sh: 1: aarch64-linux-gnugcc: not found` |
| No `sudo` on this machine (lessons 35–36) | `sudo -n true` and every `sudo apt-get` **time out**. Consequence: no `mount`, no package installs, so `dosfstools`/`mtools` are unavailable. Unprivileged disk images are built with `truncate -s 128M disk.img` + `mkfs.ext4 -F -q -L BOOT disk.img` + `debugfs -w -R "write SRC DST" disk.img` — verified working; U-Boot reads that image with both `ext4load` and plain `load` |
| U-Boot environment on `virt` (lesson 35) | `CONFIG_ENV_IS_IN_FLASH=y`, `CONFIG_ENV_ADDR=0x4000000`, `CONFIG_ENV_SIZE=0x40000` (**256 KiB**). With `-bios` the env is RAM-backed flash: `saveenv` + `reset` **keeps** the value, a brand-new QEMU process **loses** it (`*** Warning - bad CRC, using default environment`). With real files — `truncate -s 64M flash0.img` + `dd if=u-boot.bin of=flash0.img conv=notrunc`, `truncate -s 64M flash1.img`, `-drive if=pflash,format=raw,index=0/1` — it survives across processes (`Loading Environment from Flash... OK`). On-disk format is a CRC32 followed by NUL-separated `name=value`, readable with `od -A d -t x1z -N 32 flash1.img` |
| QEMU slirp + TFTP (lesson 36) | `-nic user,model=virtio-net-device,tftp=…` **does not work** on `-M virt`: two warnings (`netdev #netNNN has no peer`, `requested NIC (anonymous, model virtio-net-device) was not created (not supported by this machine?)`) then `Net:   No ethernet found.` The working form is the explicit pair `-netdev user,id=net0,tftp=$HOME/… -device virtio-net-device,netdev=net0` → `Net:   eth0: virtio-net#31`. Addresses are fixed: guest **10.0.2.15**, host + TFTP server **10.0.2.2**, gateway 10.0.2.2, netmask 255.255.255.0. Measured **24.5 MiB/s** for the 30 771 136 B `Image` and **21.5 MiB/s** for the 1 035 397 B initramfs. `dhcp` with default `autoload` also chases `'0A00020F.img'` (= 10.0.2.15 in hex) — `setenv autoload no` first |
| `-machine dumpdtb` depends on the WHOLE command line (lesson 36) | Same `-M virt -cpu cortex-a57 -m 512`, two dumps: **without** `-bios` → **393** dts lines, `pl061@9030000` present (count 1) plus a `gpio-keys` node; **with** `-bios u-boot.bin` (+ the netdev pair) → **372** lines, `pl061` count **0**. A FIT built from the first DTB kills the kernel in `amba_read_periphid` (`Internal error: synchronous external abort: 0000000096000010`, `x9 : 0000000009031000`, trace `of_platform_populate` → `amba_device_add`) with **no console output at all** unless `earlycon=pl011,0x9000000` is added to `bootargs`. U-Boot confirms independently: `fdt addr ${fdtcontroladdr}` then `fdt print /pl061@9030000` → `libfdt fdt_path_offset() returned FDT_ERR_NOTFOUND`. The dump is also **not reproducible** — QEMU injects a fresh `rng-seed`/`kaslr-seed` each time, so the DTB sha256 differs on every run. The file is always padded to **1 048 576 B** |
| FIT image on this machine (lesson 36) | `.its` with three `images` (kernel `load`/`entry` `0x40400000`, `flat_dt`, `ramdisk`, each carrying `hash-1 { algo = "sha256"; }`) plus one `configurations/conf-1`; `mkimage -f kernel.its tftp/kernel.itb` → **32 856 448 B** from 30 771 136 + 1 048 576 + 1 035 397 = 32 855 109, i.e. **1 339 B** of FIT metadata (**0.004 %**). FIT hash values are byte-identical to host `sha256sum` (`Image` = `dce50333…5a40`, initramfs = `470a9142…a701`). Loading the FIT at `${kernel_addr_r}` fails with `ERROR: new format image overwritten - must RESET the board to recover` because the kernel `load` address is the same — use `0x44000000`. Flipping byte 5000000 from `e1` to `ff` → `sha256 error!` / `Bad hash value for 'hash-1' hash node in 'kernel-1' image node` / `Bad Data Hash` / `ERROR -2: can't get kernel image!` |
| FIT signing (lesson 36) | `openssl genpkey -algorithm RSA -pkeyopt rsa_keygen_bits:2048` + `openssl req -batch -new -x509` → `keys/dev.key` **1 704 B** (`-rw-------`), `keys/dev.crt` **1 127 B**. Put `signature-1 { algo = "sha256,rsa2048"; key-name-hint = "dev"; sign-images = "kernel", "fdt", "ramdisk"; }` inside `conf-1`, then `mkimage -f kernel-signed.its -k keys -K control.dtb -r` (control.dtb = a copy of the `-bios` DTB dump). control.dtb grows 1 048 576 → **1 049 984 B** and gains `/signature/key-dev` with `required = "conf"`, `rsa,num-bits = <0x800>`, `rsa,exponent = <0x00 0x10001>`. QEMU `-dtb control.dtb` hands it to U-Boot as the control FDT (works because `CONFIG_OF_BOARD=y`). Signed image → `Verifying Hash Integrity ... sha256,rsa2048:dev+ OK` three times, then boots; the **intact but unsigned** image on the same U-Boot → `No 'signature' subnode found for 'conf-1' config node` / `Failed to verify required signature 'key-dev'` / `Bad Data Hash` / `ERROR -2` |
| `/bin/sh` is dash, and it matters for every script lesson (lessons 12–13, bt-13) | `/bin/sh -> dash` (`lrwxrwxrwx 1 root root 4 Feb  3  2026`), `realpath /bin/sh` → `/usr/bin/dash`, `dash --version`-equivalent from dpkg = **0.5.12-12ubuntu3**; bash is **5.3.9(1)-release**. Consequences measured for real: `sh script.sh` **ignores the shebang entirely**; a `[[ ]]` under dash prints `[[: not found`, the `if` takes the **else** branch, and the script **still exits 0** — a silent wrong result; a bash array `a=(x y)` under dash is a hard `Syntax error: "(" unexpected`, rc=**2**, but the lines *before* it have already run, because **dash parses and executes one command at a time** (no whole-file parse). `local` **does** work in dash even though POSIX does not require it. Refusal codes: no `+x` → **126**, missing file → **127**, `#!/bin/nosuchshell` → `bad interpreter` **126** |
| `set -e` exception surface, measured (bt-13) | A function that always `return 1`, called four ways under `set -e`: inside `if` → survives; left of `&&` → survives; non-final stage of a pipeline → survives; **bare and unchecked → script dies, rc=1**. The rule that fits all four: `set -e` only fires when the exit status would be **thrown away** |
| `PIPESTATUS` is destroyed by the very next command (bt-13) | `false \| tee log >/dev/null` then `echo "$? / ${PIPESTATUS[*]}"` **in one command** → `$? = 0`, `PIPESTATUS = (1 0)`; with `set -o pipefail` → `$? = 1`, same `(1 0)`; three stages with the middle failing → `$? = 1`, `(0 1 0)`. But reading `$?` on its **own line first** (`rc=$?`) resets `PIPESTATUS` to **`(0)`** — the assignment is itself a command. **A first probe run of this got `(0)` everywhere and was wrong**; always expand `$?` and `PIPESTATUS` inside the *same* command |
| A function's exit status is 8-bit and lossy (bt-13) | `return 300` → `$?=44`; `return -1` → `$?=255`. `n=$(count_c)` where `count_c` ends in `return "$n"` yields `n=''` (the function echoes nothing) while `$?`=3. Under `set -e` that same assignment **kills the script at that line with rc=3 and no output at all** — the exit status of `var=$(cmd)` is the exit status of `cmd`. Confirmed causal: adding a 4th `.c` file changed rc to 4 |
| A missing `local` can be invisible (bt-13) | A function that clobbers the caller's `i`, called through `$( )`, leaves the caller's `i` **untouched** (99) — the command substitution runs it in a **subshell**. Called directly, the same function destroys it (`i=3`). Investigated because the defect appeared not to reproduce; it does reproduce, just not through `$( )` |
| `[` is a builtin *and* a file (bt-13, ties to lesson 4) | `type -a [` → `[ is a shell builtin` / `[ is /usr/bin/[` / `[ is /bin/[`; `ls -l /usr/bin/[` → `lrwxrwxrwx 1 root root 28 Mar 30 23:50 /usr/bin/[ -> ../lib/cargo/bin/coreutils/[` (uutils, the Rust coreutils — one binary, many names via `argv[0]`, same idea as BusyBox). `type -a echo` likewise shows builtin + `/usr/bin/echo` + `/bin/echo`. `[[` appears in **no** listing — it is a bash **keyword**, which is why dash reports `[[: not found` |
| `/tmp` is wiped between WSL sessions | A scratch dir created by one `wsl … bash -c` invocation is **gone** by the next one if the distro shut down in between (`/tmp/bt13-scratch: No such file or directory`). Probe scripts must be **self-contained**: recreate their own scratch dir and their own fixture files every run, never rely on a previous probe's leftovers |
| apt metadata timestamps mean two different things (lesson 12 / bt-12) | On `/var/lib/apt/lists/*`, `mtime` is the **server's publication date** (preserved on download) while `ctime` is when this machine wrote the file. They differ by days. This release's `InRelease` carries **no `Valid-Until` field**, so "the index expired" cannot be demonstrated on it. Two `dpkg-cross`-family packages have **no `Installed-Size`** at all, so any exercise summing that field must tolerate a missing key |
| apt timing is dominated by the solver, not by I/O (lesson 12 / bt-12) | `apt-cache show <pkg>` is **~0.05 s** and stable; `apt-get -s install <pkg>` varies **0.9–2.2 s** across runs on the same package. Never quote a single `apt-get -s` timing as a fixed number in a lesson |
| Reading apt state without `sudo` (lesson 12 / bt-12) | With no working `sudo` on this machine, `apt-get update` is impossible, but read-only work is not: point apt at a user-owned state dir with `-o Dir::State::Lists=…` / `-o Dir::Cache=…` under `$HOME`, and `apt-cache`/`apt-get -s` work against the system lists unchanged |
| **`/proc/config.gz` exists and is readable** (lesson 37) | **50 653 B**, world-readable — the WSL2 kernel is built with `CONFIG_IKCONFIG_PROC=y`. This is a lucky break, not universal: most embedded kernels omit it, and then there is *no* way to recover the config from the running system. `zcat /proc/config.gz` → **8 757** lines, **2 176** `=y`, **980** `=m`, **3 620** `# … is not set`. Only `config.gz` is compressed; `zcat` on any other `/proc` file → `not in gzip format` |
| Module inventory of the WSL2 kernel (lesson 37) | `/lib/modules/$(uname -r)/kernel` holds **961** `.ko` files, **157 MB** on disk, but `lsmod` shows only **15** loaded, while `modules.builtin` lists **349** modules welded into the image at build time. Largest single module is `amdgpu.ko` at **22.9 MB**. Scan `/lib/modules/$(uname -r)/**kernel**`, never the parent — `find` on the parent hits `lost+found: Permission denied` |
| `/proc/kallsyms` is stable within a boot, drifts across boots (lesson 37) | **211 171** lines, **167 197** of type `T`/`t`. Eight samples 1 s apart in one session were **byte-identical**, and `comm -23`/`comm -13` on two dumps 2 s apart showed **zero** differing symbols. Across boots it moves by one or two, because the table includes symbols of **loaded modules and running BPF programs** (**395** `bpf_prog_`/`ftrace_trampoline` symbols present). Quote the order of magnitude, not the exact number. Every address reads `0000000000000000` — `kptr_restrict` = 1, protecting KASLR; names are still complete. `grep -c ' [Tt] vfs_write'` **without** the `$` anchor returns **2** (it also matches `vfs_writev`) |
| vDSO vs forced syscall, `clock_gettime` ×1 000 000 (lesson 37) | vDSO **≈19 ns/call**, `syscall(SYS_clock_gettime, …)` **≈159 ns/call** → **8.6× / 7.8× / 8.3×** over three runs. The decisive proof is not the timing: the program makes 2 000 000 calls but `strace -c` counts exactly **1 000 000** — the vDSO half never crosses the boundary. `ldd` on the binary lists `linux-vdso.so.1 (0x…)` with **no path**, and `/proc/self/maps` shows `[vdso]` (one page, `r-xp`) + `[vvar]`. **Never time anything under `strace`**: the same program under `strace` reports `ratio 6394.4x` because `ptrace` stops each syscall twice (≈98 µs each) |
| `user`/`sys` split as a diagnostic (lesson 37) | `spin` (200 M float divisions, no syscalls) → `real 0m1.5s user 0m1.5s sys 0m0.0s`, **0.7 %** in the kernel. `chatty` (2 000 000 × `write(fd, "x", 1)` to `/dev/null`) → **78 %** in `sys`. Same wall clock, opposite fix: the first needs better code, the second needs *fewer* trips into the kernel. `gcc -Wall` rejects `write(...)` with the result ignored (`warn_unused_result`) — accumulate into a `long` |
| Driver model as seen from `/sys` (lesson 37) | **34** buses in `/sys/bus`, **69** classes in `/sys/class`. Every `/sys/bus/virtio/devices/virtioN` has a `driver` symlink into `/sys/bus/virtio/drivers/…` and a `modalias` of the form `virtio:d00000010v00001AF4` (`d` = device type, `v` = vendor). That string is what the kernel puts in the uevent and what `modprobe` matches against `modules.alias` |
| `/proc` and `/sys` files report `st_size` = 0 (lesson 37) | `ls -l /proc/uptime` and `ls -l /proc/kallsyms` both say **0**, yet the second yields >200 000 lines — the content is generated at read time. Never size a buffer from a `/proc` or `/sys` `stat`. Two `cat /proc/uptime` one second apart differ, proving the same point |
| `stat -f` misreports ext4 as `ext2/ext3` (lesson 37) | The three ext generations share one filesystem magic and the Rust coreutils on this machine print the shared name. Use `df -T ~` or `findmnt -no FSTYPE ~` when the exact type matters. Related trap: **`/tmp` is `tmpfs`**, so a file created there cannot stand in for an ext4 file — put the ext4 demo under `$HOME` |
| ftrace is root-only here (lesson 37) | `/sys/kernel/tracing` is `drwx------ root root`; `cat available_tracers` → `Permission denied`, and there is no working `sudo`. Any lesson before Chặng 10 must read the kernel through `/proc` and `/sys` instead |
| GCC 15 turned two old warnings into hard **errors** (lesson 15 / bt-15) | `implicit declaration of function 'f' [-Wimplicit-function-declaration]` is an **error** by default here, and stays an error even under `-std=gnu17` — GCC 14+ promoted it regardless of the standard, and `__STDC_VERSION__` is **202311** anyway. The old two-step story ("warning at stage 2, then `undefined reference` at stage 4") therefore **cannot be reproduced on this machine**: the build stops at stage 2. Lesson 15's `Lỗi thường gặp` row was corrected on 2026-08-19. Likewise `static int f(int)` after a **non-static** declaration of `f` in a header is `error: static declaration of 'f' follows non-static declaration` — so a "static hides the symbol" demo must declare the function in the *caller*, never in the shared header |
| `sqrt()` with a constant argument never reaches the linker (lesson 15 / bt-15) | The classic "forgot `-lm`" demo **does not reproduce** if the argument is a literal: `sqrt(2.0)` is constant-folded at stage 2, `nm root.o` shows **no `U sqrt` at all**, and `gcc -o root root.o` links fine with no `-lm`. Feed the value in at run time (`atof(argv[1])`) and `U sqrt` appears, the link fails with `undefined reference to 'sqrt'`, and `-lm` fixes it. Any exercise about library linking must use a **runtime** argument |
| `gcc -save-temps` size curve for a three-header program (bt-15 E1) | **These numbers belong to one exact source — do not re-measure with a paraphrase of it.** The `calc.c` they were taken from is the 223-byte / 11-line file whose `main` body is `char buf[32]; snprintf(buf, sizeof buf, "%d", 6 * 7); printf("answer = %s (len %zu)\n", buf, strlen(buf)); return EXIT_SUCCESS;` (headers `stdio.h` + `stdlib.h` + `string.h`). A trimmed variant — `"%d", 42`, `printf("len = %zu\n", …)`, `return 0` — is 192 B and gives **63 502 / 1 062 / 1 784 / 16 072** with **282** `#` lines and 11/2 329/69 lines, i.e. every figure below moves except the executable. bt-15 E1/E6, B2's `nm` output and D2's "2 333 dòng" are all pinned to the 223-byte version. Curve: `.i` **63 588 B** / **2 333** lines / **284** `#` marker lines / **73** headers → `.s` **1 096 B** / **70** lines → `.o` **1 800 B** → executable **16 072 B**, and `-static` → **816 856 B**. `ldd` on the dynamic one lists `linux-vdso.so.1`, `libc.so.6`, `/lib64/ld-linux-x86-64.so.2`; `nm calc.o \| grep ' U '` → `__stack_chk_fail`, `printf`, `snprintf`, `strlen` |
| Which macro traps GCC 15 warns about, and which it does not (bt-15 E2) | Measured on fresh macros: `AREA_BAD(w,h) w*h` called as `AREA_BAD(1+1, 2+2)` expands to `1 + 1 * 2 + 2` → **5** (correct is 8) with **no warning at all**; `HALF_BAD(x) (x)/2` used as `100 / HALF_BAD(10)` expands to `100 / (10) / 2` → **5** (correct is 20), again **no warning**. Only the double-evaluation trap is caught: `TWICE(x) ((x)+(x))` called as `TWICE(n++)` with `n=3` gives **7** and leaves `n=5`, and GCC does emit `warning: operation on 'n' may be undefined [-Wsequence-point]`. Lesson 15's `GREATER(i++, j)` is *not* warned about because `?:` puts a sequence point between the two uses — so "macros are silent" is true for two of the three traps, not all three |
| `ld` reports **every** link error at once; stage 2 stops at the first (bt-15 E5) | A three-defect project (`area()` *defined* in `geo.h`, `diag()` never declared, `sqrt` with no `-lm`) does **not** surface one defect per build round, which is the natural assumption when writing a "fix them in order" exercise. Round 1 fails at **stage 2 only** — `main.c:9:27: error: implicit declaration of function 'diag'` (plus a bonus `warning: format '%f' expects … but argument 2 has type 'int' [-Wformat=]`, because the implicit declaration makes `diag` return `int`), and the follow-on link then dies on `cannot find main.o` because no `.o` was produced. Round 2 reports the remaining **two together**: `` geo.c:(.text+0x0): multiple definition of `area' `` *and* `` geo.c:(.text+0x60): undefined reference to `sqrt' ``. Reason: `ld` scans all relocations and prints every unresolved one before exiting, whereas `cc1` aborts the translation unit at the first hard error. Also verified: `gcc -o app2 -lm main.o geo.o` **still fails** — `-lm` before the `.o` files is scanned when nothing needs it yet, so library position on the link line is real, not folklore |

| Kernel source 6.18.45 — download, signature, extraction (lesson 38) | `https://cdn.kernel.org/pub/linux/kernel/v6.x/`: `linux-6.18.45.tar.xz` **154 592 412 B** (147.4 MB; curl measured **26.2 MB/s**, ~5 s) + `linux-6.18.45.tar.sign` **991 B**. The signature covers the **uncompressed** `.tar`, so the only working form is `xz -cd linux-6.18.45.tar.xz \| gpg --verify linux-6.18.45.tar.sign -`. Keys via `gpg --locate-keys torvalds@kernel.org gregkh@kernel.org`: Greg KH `647F28654894E3BD457199BE38DBBDC86092693E` (rsa4096, 2011-09-23), Linus `ABAF11C65A2970B130ABE3C479BE3E4300411886` (rsa2048, 2011-09-20). `Good signature` **always** comes with `WARNING: This key is not certified with a trusted signature!` — normal, the keys are `[ unknown]` trust; do not "fix" it. Failure modes hit for real: `.tar.xz` fed straight to gpg → **BAD signature**; trailing `-` omitted → `gpg: no signed data` + `gpg: can't hash datafile: No data`; no key → `Can't check signature: No public key`. `time tar -xf` into an **empty** dir = **real 0m11.806s / user 12.674 / sys 12.566** (xz decodes multi-threaded, so user+sys > real); `xz -T0` explicitly is no faster (**12.946 s**) |
| Kernel tree census, 6.18.45 (lesson 38) | Extracted tree **1.7 GB**, **91 120** regular files + **85** symlinks (so `git ls-files` = **91 205** — two tools answering different questions, not a discrepancy). **35 993** `.c`, **26 155** `.h`, **5 988** `.dts`/`.dtsi`, and **36 277 772** lines of `.c`/`.h`/`.S` (at 200 lines/min ≈ **3 023 h**, just over a year at 8 h/day — the widely-quoted "30 million lines" is out of date). `du -sh -- */ \| sort -h`: `drivers/` **1.1 G** (≈65 %), `arch/` 161 M, `tools/` 94 M, `Documentation/` 80 M, `include/` 60 M, `sound/` 54 M, `fs/` 49 M, `net/` 38 M, `kernel/` 16 M, `lib/` 11 M, `mm/` 6.1 M, `scripts/` 4.9 M, `block/` 2.2 M, `init/` **228 K**, `certs/` 80 K. **24** directories at the root, **21** architectures under `arch/`. `MAINTAINERS` has **9 184** `F:` lines; `Documentation/devicetree/bindings/` **6 009** files; `kernel-parameters.txt` **8 418** lines; `filesystems/vfs.rst` **1 551** lines. Makefile head: `VERSION = 6` / `PATCHLEVEL = 18` / `SUBLEVEL = 45` / `EXTRAVERSION =` |
| Searching the kernel tree is page-cache-bound, not tool-bound (lesson 38) | Same `grep -rn ext4_file_write_iter`, same **two** result lines, four scopes, three runs each: whole tree **36.748 → 4.091 → 1.381 s**; `--include='*.c'` **0.776 / 0.883 / 0.837 s**; `--include='*.c' fs/` **0.053 / 0.049 / 0.043 s**; `fs/ext4/` **0.004 / 0.006 / 0.008 s**. The 26× spread in the first column is the page cache filling (1.7 GB tree vs ≈4.8 GiB of WSL2 RAM) — **always run three warm-ups before quoting a figure, and never interleave scans of two big trees**. Warm figures used in the lesson: whole tree **1.362 s** vs `fs/ext4/` **0.003 s**; the second is at the resolution floor of `time`, so the honest claim is "two orders of magnitude", not "454×". Changing *tool* buys almost nothing: inside the git tree `git grep` runs 12.988 / 7.513 / **1.062 s** against `grep -rn --exclude-dir=.git` 1.624 / 1.482 / **1.476 s** — only ≈**1.3×**, and `git grep` burns **2.4 s of sys** doing it. Narrowing the question beats swapping the tool |
| Shallow git clone of the kernel, and proof it equals the tarball (lesson 38) | `git clone --depth 1 -b v6.18.45 https://git.kernel.org/pub/scm/linux/kernel/git/stable/linux.git` = **95.1 s**, tree **2.0 G** of which `.git` is **282 M**, exactly **1** commit (`bf3be28f6721e24961992ebb9e61c0cf21a56806`, "Linux 6.18.45", Greg KH, Wed Aug 19 18:18:21 2026 +0200), `git describe --tags` → `v6.18.45`. Two harmless messages that read like errors: `warning: refs/tags/v6.18.45 … is not a commit!` (annotated tag) and the `detached HEAD` advice. `diff -rq --no-dereference linux-6.18.45 linux -x .git` prints **nothing** and exits **0** — **21.518 s** warm, **1m8.881s** cold |
| Where lesson 37's seven names actually live, in 6.18.45 (lesson 38) | `fs/read_write.c`: `SYSCALL_DEFINE3(write` **:746**, `ksys_write` **:727**, `vfs_write` **:666**. `arch/x86/entry/entry_64.S`: `SYM_CODE_START(entry_SYSCALL_64)` **:87**. `arch/arm64/kernel/entry-common.c`: `el0_svc` **:743** (`el0_svc_compat` :895). `arch/arm64/kernel/syscall.c`: `invoke_syscall` **:38**. `fs/ext4/file.c`: `ext4_file_write_iter` **:694**, `const struct file_operations ext4_file_operations` **:963**. **`__arm64_sys_write` does not exist in the source** — `grep -rn` over the whole tree exits **1**; the name is pasted together by `__SYSCALL_DEFINEx` (`__arm64_sys##name`) at `arch/arm64/include/asm/syscall_wrapper.h` **:48–58**, reached via `include/linux/syscalls.h` **:227** (`SYSCALL_DEFINE3` → `SYSCALL_DEFINEx`) and **:234** (→ `__SYSCALL_DEFINEx`). PL011 chain: `"arm,pl011"` matches exactly **one** file under `drivers/` — `drivers/tty/serial/amba-pl011.c` (**3 146** lines) — plus `Documentation/devicetree/bindings/serial/pl011.yaml`; `drivers/tty/serial/Makefile` **:30**; `Kconfig` **:48** (`SERIAL_AMBA_PL011`) and **:59** (`SERIAL_AMBA_PL011_CONSOLE`, `depends on SERIAL_AMBA_PL011=y`). `MAINTAINERS` EXT4 block **:9377–9388**. **`fs/shmem.c` does not exist** — tmpfs is `mm/shmem.c`; the second `file_operations` example is `fs/proc/inode.c` **:555** (`proc_reg_file_ops`). A real DT node using the same UART: `arch/arm64/boot/dts/arm/juno-base.dtsi` **:897–903** (Juno board, not `virt`) |
| Kconfig census, 6.18.45 (lesson 39) | **1 880** `Kconfig*` files (`drivers/` **1 336**, `arch/arm64/` **5**), holding **21 740** `config` + **385** `menuconfig` = **22 125** symbol definitions, **189** `choice` blocks, **11 646** `tristate` lines, **8 813** `bool` lines, **19 717** `depends on`, **21 923** `select`. `arch/arm64/configs/defconfig` is **1 824** lines. All counts taken with **anchored** patterns (`^[[:space:]]*config `) — an unanchored `grep` overcounts and the numbers stop adding up |
| `make ARCH=arm64 defconfig` in `~/bai38/linux-6.18.45` (lesson 39) | Cold run: **13** `HOSTCC`/`LEX`/`YACC`/`HOSTLD` lines building `scripts/kconfig/conf`, then `*** Default configuration is based on 'defconfig'` and `# configuration written to .config`. **real 0m5.964s / user 0m3.369s / sys 0m0.987s**. A warm re-run prints `# No change to .config` instead. Result: `.config` **11 727 lines / 314 649 B**, md5 **`611d4d6d025c3b7e030e1352b8efaf86`**. Breakdown: **544** blank + **6 520** comment (of which **4 950** are `# CONFIG_x is not set`) + **4 663** value lines = 11 727. Value lines split **3 273** `=y`, **1 273** `=m`, **24** strings, **91** ints, **2** hex |
| `make ARCH=arm64 syncconfig` prints **nothing**, even cold (lesson 39) | Verified by moving `include/config/` and `include/generated/` aside first: still zero output. It writes `include/config/auto.conf` **118 292 B** (what `Makefile:798` reads), `include/generated/autoconf.h` **164 560 B** with exactly **4 663** `#define`s — one per value line in `.config`, no more — and `include/config/auto.conf.cmd` **55 963 B**. `=m` becomes `#define CONFIG_X_MODULE 1` **and nothing else**: `grep -c '^#define CONFIG_BTRFS_FS 1$'` → **0**, while `#define CONFIG_BTRFS_FS_MODULE 1` is at line 918. The experiment also clobbers `include/generated/rustc_cfg` — restore it afterwards |
| `y → m → y` is not a round trip (lesson 39) | On the arm64 defconfig: `scripts/config --module SERIAL_AMBA_PL011` + `make olddefconfig` silently **deletes** `CONFIG_SERIAL_AMBA_PL011_CONSOLE` (line 3727) because `Kconfig:59` says `depends on SERIAL_AMBA_PL011=y`. Switching back with `--enable` + `olddefconfig` restores the driver at `=y` but leaves `# CONFIG_SERIAL_AMBA_PL011_CONSOLE is not set` — `diff` against the original reports exactly `3727c3727`. The resulting kernel builds, has the UART driver, and prints **no boot log at all** |
| `select` cannot be overridden from `.config` (lesson 39) | `scripts/config --disable SERIAL_CORE` does write `# CONFIG_SERIAL_CORE is not set` at line 3751, but the next `make ARCH=arm64 olddefconfig` puts it straight back to `CONFIG_SERIAL_CORE=y` — **72** `select SERIAL_CORE` lines in `drivers/tty/serial/Kconfig` demand it. `scripts/config` accepts either `SERIAL_CORE` or `CONFIG_SERIAL_CORE`; lines 8 and 61–62 of the script strip the prefix |
| `savedefconfig` and the version-bump workflow (lesson 39) | `make ARCH=arm64 savedefconfig` turns the **11 727-line / 314 649 B** `.config` into `./defconfig` **1 755 lines / 42 448 B** (**7.4×** smaller), keeping only what differs from the Kconfig defaults; it starts at `CONFIG_SYSVIPC=y`. Round trip `cp defconfig .config && make ARCH=arm64 olddefconfig` reproduces the original **byte for byte** (`diff -q` silent). Simulating a kernel upgrade by deleting the `CONFIG_BTRFS_FS=m` line makes `make ARCH=arm64 listnewconfig` print exactly one line, `CONFIG_BTRFS_FS=n`; `olddefconfig` then writes `# CONFIG_BTRFS_FS is not set`, and a second `olddefconfig` says `# No change to .config`. `make ARCH=arm64 help` lists **24** Configuration targets, `config` through `testconfig` |
| **`cat` uses `splice()`, not `read`/`write`** (lesson 19 / bt-19) | Consequence of the "Coreutils = uutils" row above, and it breaks the most natural syscall demo there is. `cat --version` → `cat (uutils coreutils) 0.8.0`. `strace -c -e trace=read,write cat /etc/hostname` counts **zero `write`** — 18 read / 12 openat / 13 close only. The full trace on a 9-byte file: `openat(…"/etc/hostname"…) = 3` · `splice(3, NULL, 1, NULL, 1048576, 0) = -1 EINVAL` (both ends are regular files; `splice` needs one end to be a pipe) · then it makes its own pipe and does `splice(3,…,5,…) = 9` · `splice(4,…,1,…) = 9` · `splice(3,…,5,…) = 0`. The data never enters user space. **Use `dd if=… of=… bs=4096` when a lesson needs a plain read/write trace** — measured **20 read + 1 write = 21**. This also explains syscall-total drift against older captures (142/6 vs 137/5): system-binary counts move with package updates, so never pin an exercise to the absolute total of a binary you did not compile |
| glibc ignores the size you pass to `setvbuf(f, **NULL**, …)` (lesson 19 / bt-19) | `setvbuf(f, NULL, _IOFBF, size)` lets glibc allocate, and it then uses **`st_blksize`** (4096 here) regardless of `size`. A buffer-size experiment written that way reports the *same* syscall count for every size and looks broken. Pass a real buffer — `static char big[1048576]; setvbuf(f, big, _IOFBF, size);` — and the sizes are honoured. Measured on a 2 288 890-byte output (200 000 `fprintf`): buffer **4096 → 559** `write()`, **65536 → 35**, **1048576 → 3**, `_IONBF` → **200 000**; wall 0.01 s buffered vs **0.09 s** unbuffered; all four output files byte-identical. Arithmetic checks exactly: 2288890/4096 = 558 r 3322, /65536 = 34 r 60666, /1048576 = 2 r 191738, plus one final flush at `fclose` |
| A **blocking** fd never short-writes — the demo needs `O_NONBLOCK` (lesson 19 / bt-19) | Expected a short `write()` from a 1 MiB write into a 64 KiB pipe; got **1 call for the whole 1 MiB**, both to a pipe and to a regular file, because the kernel loops internally while the fd is blocking. To capture a real short write, set `O_NONBLOCK`: `fcntl(fds[1], F_GETPIPE_SZ)` = **65536**, ask for 1 048 576 → call 1 returns **65536** with `errno = 0 (Success)`, call 2 returns **-1** with `errno = 11 (EAGAIN)`. So short writes are demonstrable only on non-blocking fds, sockets, serial lines, or when a signal interrupts a partially-completed write |
| Syscall cost and `errno` staleness, measured (lesson 19 / bt-19) | Reading the same 2 288 890-byte file (warm in page cache) with `read()`: chunk=**1** → 2 288 890 calls, wall **0.45 s** → **≈196 ns per syscall**; chunk=4096 → 559 calls, chunk=65536 → 35 calls, both **0.00 s**. `errno` staleness in one run: a *successful* `open("/etc/hostname")` returning fd 3 still reports `errno=2 (ENOENT)` left over from the previous failed `open`; `isatty()` on a regular file **succeeds** (returns 0) yet sets `errno=25 (ENOTTY)`, which survives a successful `close()`. Also captured: `/dev/full` → `write()` returns −1, `errno = 28 (ENOSPC)`, and `echo hello > /dev/full` → `bash: echo: write error: No space left on device`, exit **1**; sparse file (`write "HEAD"`, `lseek` 100 MiB, `write "TAIL"`) → `ls -l` **104857604**, `du -h` **8.0K**, `du --apparent-size` **101M**, `stat` blocks=16. **Never quote `strace -c`'s "seconds" column** — ptrace inflates it (2.70 s for a run whose wall clock was 0.09 s); use wall clock and call counts |
| `scripts/get_maintainer.pl` works on a tarball tree without `--nogit` (lesson 38) | An earlier draft of lesson 38 claimed the script *errors* on a non-git tree unless given `--nogit --nogit-fallback`. **It does not** — verified 2026-08-20: it silently skips the git heuristics and prints the same four lines for `fs/ext4/file.c` (Theodore Ts'o, Andreas Dilger, `linux-ext4@vger.kernel.org`, `linux-kernel@vger.kernel.org`). The flags only make the output deterministic and fast || glibc's `sleep()` **truncates** the remaining seconds (lesson 20/21 probes) | `sleep(5)` interrupted by a signal at exactly 1.000 s returns **3**, not 4 — the return value is `ceil`-free integer seconds, so the fractional remainder is thrown away. The idiom `while ((n = sleep(n)) > 0);` therefore loses up to a second *per interruption* and collapses entirely under a signal burst: a `bad_reaper`/`good_reaper` pair written that way reaped **all** children in both variants and hid the bug it was meant to show. Use `for (int i = 0; i < 30; i++) usleep(100000);` when a program must stay alive for a fixed wall-clock span while taking signals |
| `execvp` falls back to `confstr(_CS_PATH)` when `PATH` is unset (bt-20 C4) | `env -i ./myexec ls` **succeeds** — POSIX has `execvp` use a system default path when the environment has no `PATH`, and `getconf PATH` here is **`/bin:/usr/bin`**. So "empty environment breaks `exec`" is false for anything in `/bin` or `/usr/bin`; the failure is *selective* and only hits binaries in non-standard directories. Verified with `~/bt20/opt-bin/mytool`: with opt-bin on `PATH` → `tool v1 running` / exit 0; under `env -i` → `execvp: No such file or directory` / exit **127**. Any exercise about `init`-vs-shell `PATH` must use a non-standard directory |
| `/proc/sys/kernel/pid_max` on WSL2 = **4194304** (bt-20 C5) | Not the 32768 you may assume. 32768 = `PID_MAX_DEFAULT` (`0x8000`), the kernel *compile-time* default that a small embedded config typically keeps — quote it as the board's value, never as this machine's. Consequence for PID-exhaustion arithmetic: a leak of one zombie per 5 s kills a 32768-PID board in **≈45.5 h**, and this machine in **≈243 days** — which is exactly why the bug cannot be found on the development host |
| `script -qec CMD /dev/null` races with orphaned grandchildren (bt-20 D1) | Used to give a program a real pty. Ran `script -qec './buffer_fork' /dev/null` **6 times: 5 gave 3 lines, 1 gave 2** — `script` tears down the pty when its direct child exits, and a forked grandchild's last `write()` can be lost. **Never pin an exercise to a `script -qec` line count.** For the stdio-buffer-duplication demo use two pipes instead and vary only the buffering: `./buffer_fork \| cat` → **4** lines (`A B A B`) and `stdbuf -oL ./buffer_fork \| cat` → **3** lines (`A B B`), both **6/6 deterministic**. That pair is also the better experiment — one variable instead of two |
| Warm cost of `fork`+`exec` on this machine (bt-20 B4/E4) | After one warm-up pass: `time (for i in $(seq 500); do /bin/true; done)` → **0.329 / 0.279 / 0.322 s** (≈**0.6 ms** per fork+exec+wait) against `for … do :; done` → **0.002 s** ×3, i.e. **≈150×**. The **first** run after a cold WSL2 start is **0.7–0.8 s**, and earlier captures in the same session drifted 0.428–0.453 s — the *ratio* is stable, the absolute figure is not, so always state hot-vs-cold. Must be `/bin/true`, not `true`: bash has a `true` builtin and the measurement silently becomes 1× |
| Absolute fork/exec timings drift ~2× **between sessions** (2026-08-26) | `fork_cost.c` (500 iterations, `CLOCK_MONOTONIC`) measured on two different days on the same machine, same binary, both idle: session A gave `fork + exit` ≈ **230 µs** / `fork + exec + exit` ≈ **830 µs**; session B, six consecutive warm runs after a warm-up (`nproc=6`, loadavg 0.07), gave **120–165 µs** / **428–491 µs**. Each session was internally consistent (A's shell loop = 0.428 s/500 ≈ 0.86 ms, B's = 0.260–0.288 s/500 ≈ 0.55 ms), so this is host/VM state, not a measurement error. **The ratio `exec` ≈ 3× `fork` held in both (3.6× vs 3.3×).** Rule for lessons: quote fork/exec cost as an *order of magnitude plus a ratio*, never as a single µs figure a learner is expected to reproduce |
| `/proc/PID/status` — **`SigPnd` is per-thread, `ShdPnd` is process-wide** (2026-08-26) | Sending a blocked signal with `kill -USR1 <pid>` leaves `SigPnd: 0000000000000000` and sets **`ShdPnd: 0000000000000200`** instead, because `kill(pid)` is *process*-directed and only `pthread_kill()` targets a thread's own queue. Reading `SigPnd` alone therefore reports "nothing pending" when a signal *is* pending — a wrong answer that looks like a correct one. **Always grep `SigPnd\|ShdPnd` together.** Same probe also proves signals do not queue: after **four** `kill -USR1`, `ShdPnd` is still `…0200`, identical to after one. Bit *n* of the word = signal *n+1*, so `0x200`=SIGUSR1(10), `0x4002`=SIGINT(2)+SIGTERM(15), `0x4`=SIGQUIT(3) — the last is inherited, bash sets SIGQUIT to ignore in background jobs |
| `bad_reaper` (`if` instead of `while` in a SIGCHLD handler) is **non-deterministic** (2026-08-26) | 5 children, one SIGCHLD handler that reaps with `if (waitpid(…) > 0)`. Across 5 runs it reaped **2 or 3** and left **3 or 2** zombies — never 5, never 0. Do not quote a fixed number in a lesson or exercise: the *variability itself* is the point (several SIGCHLDs coalesce into one delivery, so one reap per delivery is not enough). `good_reaper` (`while`) reaped **5/5** and left **0** zombies in 4/4 runs |
| `race_unsafe` (lesson 23) finishes in **~3 ms**, so `timeout -s KILL` cannot catch it mid-run (bt-23 E4, 2026-08-27) | `time ./race_unsafe` → `real 0m0.003s`. `timeout -s KILL 0.05` and even `timeout -s KILL 0.002` (5 attempts) all gave `exit=0` with `/dev/shm` empty: the process finishes and runs its own `shm_unlink` before the signal lands. **To demonstrate a leaked shm region you need a program that deliberately stands still** — `shm_open` + `ftruncate` + `mmap` + `memset`, then `pause()`, run with `&` and killed via `kill -9 $!`. That reproduces 100 % of the time |
| A racy run **can come out exactly right** (bt-23 E2, 2026-08-27) | 15 runs of `race_unsafe` (expected `400000`) gave `lost` anywhere from **0 to 200 000** — including one run of `actual 400000, lost 0`. Lesson 23 records only 272 646 / 204 146 / 203 165 (32–49 % lost), which is a narrower band than reality. Never write an exercise whose correct answer is "the numbers will all differ": a single clean run is a real outcome, and it is the whole reason *chạy thấy đúng không phải bằng chứng* (bt-22 trục) matters |
| `/dev/shm` clean baseline drifted to **`none 2.5G 0 2.5G 0%`** (2026-08-27) | Lesson 23 captured `none 2.5G 4.0K 2.5G 1%`. Anything quoting the 4,0K figure as "the clean-machine baseline" is now wrong. Related: **`ftruncate` alone never moves `df`** — it sets the size, tmpfs allocates no page. `ls -l /dev/shm/` shows the full size while `df` still reads `0`; only touching the pages (`memset` over the mapping) makes the cost visible. A 64 MB leaker moves `Used` from `0` to `64M`, and it stays at `64M` after `kill -9` until someone `rm`s the file |
| Opening a FIFO `O_RDONLY` blocks **before** any `read` (bt-23 E6, 2026-08-27) | A reader that does `open(a)` → `open(b)` → loop stops at the *first* `open` and never reaches the second, so the classic "two blocking FIFOs" demo fails one step earlier than expected. Writing only to `b` produces **no output at all** (the `echo` into `b` blocks too, waiting for a reader). Writing to `a` releases everything at once: `opened a = 3` · `opened b = 4` · `from a: a1` · `from b: b1`. Any lesson 24 `select`/`poll`/`epoll` motivation must open with `O_RDWR` or `O_NONBLOCK` to get past this |
